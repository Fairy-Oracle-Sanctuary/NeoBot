# 未来计划 (Roadmap)

> 本文档记录 Calglau BOT (NEO Bot Framework) 的未来发展方向与规划。
> 状态标记：[已完成] / [进行中] / [规划中] / [构想中]
> 欢迎参与讨论：在 GitHub Issues 提出建议，或直接提交 PR。

---

## 一、插件系统解耦

**现状**：所有插件内嵌于 `src/neobot/plugins/`，与框架核心同仓同版本发布。插件加载依赖 `plugin_manager` 的目录扫描，插件代码与框架代码在物理上没有边界。

**目标**：插件与框架核心彻底解耦，让"写插件"和"改框架"互不干扰。

### 1.1 插件 API 契约化 [进行中]

- ✅ 已建立 `neobot.plugin_api` 契约命名空间(plugin-api-v1):注册装饰器 / 事件模型 / Bot / 权限 / 服务 / 清单统一从该命名空间导入,契约版本见 `plugin_api.API_VERSION`
- ✅ 清单标准化:`define_plugin()` 声明插件名 / 描述 / 用法 / 版本 / 作者 / 契约版本,替代旧 `__plugin_meta__` 字典(旧字典仍兼容)
- ✅ import 边界检查:插件加载时 AST 扫描源码,新式契约插件直接 import `neobot.core.*` / `neobot.adapters.*` 会**拒绝加载**;旧式插件聚合警告,迁移期兼容
- ✅ 契约文档 `docs/plugin-api.md` + 插件开发指南全部按契约写法更新;`echo.py` 已迁移为示范实现
- ⏳ 剩余插件(约 38 个)逐步迁移到契约写法;框架升级时保证 v1 向后兼容,不兼容变更提升契约大版本并提供迁移工具

### 1.2 插件独立打包 [规划中]

- 插件从 `src/neobot/plugins/` 迁出，每个插件成为独立目录/包，自带 `manifest`（插件名、版本、作者、依赖、入口）
- 插件可独立版本化，不再跟随框架整体发布
- 框架启动时从"插件源"（本地目录 / 远程仓库 / 用户自定义路径）发现并加载插件

### 1.3 热插拔能力 [构想中]

- 基于现有 `plugin_manager` 的 reload 机制，扩展为运行时安装 / 卸载 / 更新 / 启用 / 禁用
- 插件卸载时自动注销其注册的命令、事件处理器与定时任务，不残留状态
- 插件更新失败时自动回滚到上一版本

### 1.4 插件隔离 [构想中]

- 可选：为第三方插件提供进程级或容器级隔离（复用现有 `code_py.py` 沙箱与 `sandbox.Dockerfile` 的经验）
- 隔离插件对核心资源（文件系统、Redis / MySQL、网络）的访问权限
- 低信任度插件默认降权运行，高信任度插件可申请提权

---

## 二、自制包管理器

**现状**：插件只能手动拷贝进 `plugins/` 目录，无版本概念，无依赖管理，更新靠人肉替换。

**目标**：像 pip / npm 一样管理插件，一条命令完成安装、更新、卸载。

### 2.1 插件包格式规范 [规划中]

- 定义 `.nbpkg`（NEO Bot Plugin Package）打包格式：插件代码 + manifest + 资源文件的压缩包
- manifest 声明：插件名、版本、最低框架版本、插件 API 版本、依赖列表、入口文件、权限需求
- 提供打包命令：`neobot plugin pack <目录>` 生成标准包

### 2.2 插件仓库 (Registry) [规划中]

- 搭建官方插件索引仓库（GitHub 仓库或独立 Registry 服务），收录已审核插件
- 插件发布流程：作者提交 -> 自动检查（manifest 校验 / API 兼容性 / 基础安全扫描）-> 上架
- 支持私有源：自建索引或指向本地目录，满足内部插件分发

### 2.3 安装器 CLI [规划中]

- 命令设计（挂载在现有 `cli.py` / 新增 `neobot` 子命令）：
  - `neobot plugin install <name|url>` 安装插件
  - `neobot plugin uninstall <name>` 卸载插件
  - `neobot plugin update [name]` 更新插件（检查依赖冲突）
  - `neobot plugin list` 列出已安装 / 可更新插件
  - `neobot plugin search <keyword>` 搜索插件
  - `neobot plugin info <name>` 查看插件详情
- 依赖解析：安装时自动拉取依赖插件，冲突时给出明确提示，支持 `--force` 覆盖

### 2.4 安全与校验 [规划中]

- 安装时校验插件包哈希与签名，防止篡改（与框架现有的凭据安全基线一致）
- 插件安装前展示权限清单（用到的能力、访问的资源），由管理员确认
- 高危操作（写文件、执行命令、外发网络请求）在 manifest 中显式声明，安装时醒目提示

---

## 三、可维护性提升

**现状**：核心与插件同仓，部分插件单文件体量较大（如 `web_parser`、MCC 系列），文档分散在 `docs/` 与 `src/neobot/docs/` 两处。

**目标**：让新开发者一天内能看懂结构、三十分钟能写出第一个插件。

### 3.1 代码分层重构 [进行中]

- 持续推进"核心 / 适配器 / 插件"三层分离，核心目录 `core/` 保持"非请勿动"的稳定边界
- 拆分过大的单文件插件为包结构（参考 `web_parser/`、`discord_cross/` 的目录化改造）
- 消灭重复逻辑：公共能力（图片、HTTP、数据库访问）收敛到 `core/services/` 或插件公共库

### 3.2 测试与 CI 补强 [规划中]

- 为插件 API 契约建立兼容性测试套件，框架升级时自动验证所有已安装插件
- CI（现有 `docker.yml` 之外）增加：单元测试、ruff 静态检查、manifest 校验
- 建立插件示例仓库 / 模板仓库，新插件从模板生成，自带最小测试

### 3.3 文档体系整合 [规划中]

- 统一文档入口：`docs/` 作为唯一文档根，`src/neobot/docs/` 合并归位
- 补齐插件开发指南：从"零到第一个插件"的完整教程（现在已有 `plugin-development/` 基础，继续深化）
- 每个公开 API 有示例代码（现有 `api-usage-examples.md` 基础上继续扩展）

### 3.4 配置治理 [规划中]

- `config.toml` 分节治理：核心 / 平台 / 插件配置分离，插件配置支持独立文件与默认值
- 配置变更热加载（watchdog 已具备监听能力，扩展到配置热生效）
- 配置校验错误信息人性化：指出具体字段、合法取值与示例

---

## 四、平台与生态

### 4.1 平台适配器扩展 [规划中]

- 现有 QQ (OneBot v11) / Discord / CLI / MCC 基础上，按需新增适配器（如 Telegram、微信）
- 适配器接口标准化，新增平台只需实现适配层，插件无需改动即可跨平台

### 4.2 插件生态共建 [构想中]

- 插件市场页（`web_static/` 现有静态站点扩展）：浏览、搜索、一键安装指引
- 优质插件收录进官方索引，标注维护状态与兼容框架版本
- 插件作者贡献指南与最佳实践文档

---

## 五、性能与稳定性

- 持续跟进 Python 3.14 JIT 与 mypyc AOT 编译路径的优化（已有基础，继续打磨）
- 插件加载耗时纳入启动日志，慢插件给出警告
- 插件崩溃自动隔离：单个插件异常不影响核心与其它插件（异常捕获与恢复机制补强）

---

## 优先级总览

| 项目 | 状态 | 优先级 | 依赖 |
|---|---|---|---|
| 插件 API 契约化 | 规划中 | 高 | 无，可立即启动 |
| 插件独立打包 / manifest | 规划中 | 高 | 契约化先行 |
| 包管理器 CLI + Registry | 规划中 | 高 | manifest 规范 |
| 热插拔与回滚 | 构想中 | 中 | 独立打包 |
| 插件隔离沙箱 | 构想中 | 低 | 热插拔稳定后 |
| 测试 / CI 补强 | 规划中 | 中 | 无 |
| 文档整合 | 规划中 | 中 | 无 |
| 插件市场页 | 构想中 | 低 | Registry 上线后 |

---

*本 Roadmap 随项目演进持续更新，具体实现细节以对应 Issue / PR 为准。*
