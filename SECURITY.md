# 安全策略 (Security Policy)

## 支持的版本

NeoBot 以 Docker 镜像方式发布，生产环境请跟随最新构建：

| 来源 | 版本 |
|------|------|
| `ghcr.io/fairy-oracle-sanctuary/neobot:latest` | 当前 main 分支的最新构建 |
| GitHub Releases | 有重大变更时发布 |

旧版本不做单独的安全维护——发现漏洞请直接反馈，修复会进 main 并随镜像自动更新。

## 报告漏洞

**不要**开公开 Issue、不要在聊天群/论坛公开漏洞细节。请私下报告，二选一：

1. **推荐**：GitHub Security Advisory 私有报告
   （仓库页面 → Security → Report a vulnerability），只有维护者可见
2. 邮件：`2221577113@qq.com`

### 报告里尽量包含

- 漏洞类型与影响（能造成什么后果）
- 复现步骤（含最小示例，PoC 可接受）
- 影响的版本 / 部署方式（Docker、直跑等）
- 可选的修复建议

### 注意事项

- **不要**在报告里粘贴生产环境的 `config.toml`、密钥、token、证书等敏感内容
- 如果怀疑自己误把密钥提交进了公开仓库，**立即轮换该密钥**，并邮件通知维护者处理历史记录
- 涉及个人隐私（如群成员数据）的问题，描述清楚边界即可，不要附带真实用户数据

## 响应承诺

| 阶段 | 时间 |
|------|------|
| 确认收到 | 48 小时内 |
| 严重漏洞（可被远程利用 / 数据泄露） | 优先处理，尽快发修复 |
| 一般问题 | 随正常迭代处理 |

修复会以 `[verified]` 提交进 main，镜像自动构建发布。修复发布后请在**公开渠道确认修复效果**，我们默认不再公开披露漏洞细节——除非你希望署名致谢，可以在报告里注明。

## 运行者安全建议

- 用 `config.example.toml` 复制出 `config.toml`，或全部走环境变量注入（`MYSQL_*` / `REDIS_*` / `NAPCAT_WS_*` / `DISCORD_TOKEN` 等）
- 不要在生产环境以 root 直接跑；镜像内进程已有降权处理
- 定期拉取最新镜像（`docker compose pull && docker compose up -d`）
- NapCat / Discord 等外部服务的 token 泄露后，第一时间在对应平台撤销重发
